// RUN COMPLIANCE, told against the run engine's own week. The composer lives in the
// repo layer now (src/repo/run-compliance.ts) — runs are never plan items, so there
// is no applied prescription left to fall back from, and repo readers (the coach
// context, the weekly read, the underfuelling strain signal) need the same number the
// Endurance screen prints. Re-exported here so the domain surface is unchanged.
export { runComplianceRead } from "../../repo/run-compliance.js";
